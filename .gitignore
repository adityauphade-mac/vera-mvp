# dependencies
node_modules
.pnpm-store

# build
.next
.turbo
dist
out
*.tsbuildinfo

# data
/data
data/jobs_dedup.jsonl
data/generated.json
# apps/web/data/generated.json is COMMITTED — Vercel reads it directly during
# build instead of running the 188 MB preprocess step. Re-run `pnpm preprocess`
# locally and commit the updated JSON when the source data changes.

# env
.env
.env.local
.env.*.local

# misc
.DS_Store
*.log
.vercel
playwright-report
test-results
coverage
tests/e2e/audit-screens
