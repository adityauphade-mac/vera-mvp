# dependencies
node_modules
.pnpm-store

# build
.next
.turbo
dist
out
*.tsbuildinfo

# data
/data
data/jobs_dedup.jsonl
data/generated.json
# `apps/web/data/` was deleted in the JSON-removal change (see
# docs/JSON_REMOVAL_PLAN.md). The dashboard now reads from Postgres only;
# the bundled JSON snapshots that lived there have been retired. The seed
# data used by the Playwright suite lives at tests/fixtures/*.json and is
# checked in.

# env
.env
.env.local
.env.*.local
# Production secrets recovery copy — see CLAUDE.md "Production environment".
# This file MUST stay out of git and out of the Vercel upload.
.env.prod

# misc
.DS_Store
*.log
.vercel
playwright-report
test-results
coverage
tests/e2e/audit-screens
.env*.local

# Claude Code local config + installed skills (per-developer, not shared)
.claude/
.agents/
skills-lock.json

# Hyperframes render outputs — sources are tracked, MP4s are re-derived by
# `npx hyperframes render`. The canonical copies live under apps/web/public/.
hyperframes/*/vera-demo*.mp4
hyperframes/*/narration.wav
hyperframes/*/.hyperframes/
hyperframes/*/renders/
